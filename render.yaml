## render.yaml — Blueprint Solar Inspection Platform
## https://render.com/docs/blueprint-spec

services:
  # ── Backend FastAPI ──────────────────────────────────────────────────────────
  - type: web
    name: solar-inspection-platform
    runtime: docker          # Utilise le Dockerfile à la racine du repo
    region: frankfurt
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: solar-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: solar-redis
          type: redis
          property: connectionString
      - key: FRONTEND_URL
        value: "https://solar-inspection-platform.onrender.com"
      - key: DEBUG
        value: "false"
      - key: RUN_MIGRATIONS
        value: "true"

  # ── Frontend React (site statique CDN) ──────────────────────────────────────
  - type: web
    name: solar-frontend
    runtime: static
    rootDir: src/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: true
    envVars:
      - key: VITE_API_URL
        value: "https://solar-inspection-platform.onrender.com/api/v1"

  # ── Redis / Key-Value ────────────────────────────────────────────────────────
  - type: redis
    name: solar-redis
    plan: starter
    maxmemoryPolicy: allkeys-lru

# ── PostgreSQL + PostGIS ─────────────────────────────────────────────────────
databases:
  - name: solar-db
    databaseName: solar_inspection
    plan: starter
    region: frankfurt
